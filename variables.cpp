#include <iostream>

using namespace std;

int main(){
    int alpha = 67;
    double beta = 24.125;
    char charlie = 'C';
    string delta = "F-22 Raptor";
    bool echo = false;
    cout << "This is the integer variable. This variable is used to store whole numbers such as: " << alpha << endl;
    cout << "This is the double variable. This variable is used to store floating point numbers with decimals such as: " << beta << endl;
    cout << "This is the char variable meant to store single characters like: " << charlie << endl;
    cout << "This is the string variable meant to store text like: " << delta << endl;
    cout << "This is the bool variable meant to store either a TRUE(1) or FALSE(0) value like this: " << echo << endl;
    return 0;
}